name: 🚀 Next.js CI/CD with Docker Hub

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build, Test, and Push Docker Image
    runs-on: ubuntu-latest

    steps:
      # -----------------------------
      # 1️⃣ Checkout repository
      # -----------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # -----------------------------
      # 2️⃣ Setup Node.js
      # -----------------------------
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.12.2
          cache: 'npm'

      # -----------------------------
      # 3️⃣ Clean install dependencies
      # -----------------------------
      - name: Clean install dependencies
        run: |
          echo "🧹 Cleaning old dependencies..."
          rm -rf node_modules package-lock.json
          echo "📦 Installing fresh dependencies..."
          npm install

      # -----------------------------
      # 4️⃣ Rebuild native modules (for Tailwind/SWC/Linux)
      # -----------------------------
      - name: Rebuild native modules
        run: |
          echo "🔧 Rebuilding native modules..."
          npm rebuild @tailwindcss/oxide --verbose || true
          npm rebuild lightningcss --verbose || true
          npm rebuild @next/swc-linux-x64-gnu --verbose || true

      # -----------------------------
      # 5️⃣ Build Next.js app
      # -----------------------------
      - name: Build Next.js app
        run: |
          echo "🏗️ Building Next.js..."
          npm run build

      # -----------------------------
      # 6️⃣ Build Docker image
      # -----------------------------
      - name: Build Docker image
        run: |
          echo "🐳 Building Docker image..."
          docker build -t nextjs-app:latest .

      # -----------------------------
      # 7️⃣ Run smoke test container
      # -----------------------------
      - name: Run Docker container (smoke test)
        run: |
          echo "🚀 Starting container for test..."
          docker run -d -p 3000:3000 --name nextjs-container nextjs-app:latest
          echo "⏳ Waiting for container startup..."
          sleep 8
          docker ps
          docker logs nextjs-container --tail 20

      # -----------------------------
      # 8️⃣ Verify app health
      # -----------------------------
      - name: Verify app is running
        run: |
          echo "🧪 Checking app health..."
          curl --fail http://localhost:3000 || (echo "❌ App failed to start" && exit 1)

      # -----------------------------
      # 9️⃣ Push image to Docker Hub
      # -----------------------------
      - name: Push image to Docker Hub
        if: github.ref == 'refs/heads/main'
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: |
          if [ -z "$DOCKER_USERNAME" ]; then
            echo "❌ ERROR: DOCKER_USERNAME secret is missing!"
            exit 1
          fi
          if [ -z "$DOCKER_PASSWORD" ]; then
            echo "❌ ERROR: DOCKER_PASSWORD secret is missing!"
            exit 1
          fi

          echo "🔑 Logging in to Docker Hub as $DOCKER_USERNAME..."
          echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

          IMAGE_NAME="$DOCKER_USERNAME/nextjs-app:latest"
          echo "🏷️ Tagging image → $IMAGE_NAME"
          docker tag nextjs-app:latest "$IMAGE_NAME"

          echo "⬆️ Pushing image to Docker Hub..."
          docker push "$IMAGE_NAME"

          echo "✅ Docker image pushed successfully to Docker Hub!"

      # -----------------------------
      # 🔟 Cleanup
      # -----------------------------
      - name: Cleanup Docker
        if: always()
        run: |
          echo "🧹 Cleaning up containers and images..."
          docker stop nextjs-container || true
          docker rm nextjs-container || true
          docker image prune -f || true
          echo "✅ Cleanup complete."
